[package]
name = "kubectl-check"
description = "Check for kubernetes metadata before executes command"
version = "0.1.0"
edition = "2021"

[profile.release]
strip = true
lto = true
panic = "abort"
opt-level = "z"

[dependencies]
atty = "0.2.14"
yaml-rust2 = "0.9.0"
